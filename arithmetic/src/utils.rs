pub fn modular_inverse(a: i64, modulus: i64) -> i64 {
	// Implement the modular inverse function
}

pub fn irreducible_polynomials(order: usize) -> Vec<usize> {
	// Implement a function to generate irreducible polys for the given order
}
